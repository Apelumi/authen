
import 'package:authen/Screens/Signup/components/body.dart';
import 'package:flutter/material.dart';

class Signup_now extends StatelessWidget {
  const Signup_now({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(
        child: Column(
          
        ),),
    );
  }
}

